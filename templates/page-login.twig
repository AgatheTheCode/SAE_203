{% extends "base.twig" %}

{% block main %}
<!DOCTYPE html>
<body>

    <div class="wrapper">
        <h2>{{Login.h2}}</h2>
            <pre> {{dump(Login)}} </pre>
        <p>{{Login.info}}</p>

        {{Login.phperr}}

        <form {{ Login.form }}>
            <div class="form-group">
                <label>Pseudo</label>
                <input class="form-control" {{ Login.input}}>
                <span class="invalid-feedback">{{Login.username_err}}</span>
            </div>    
            <div class="form-group">
                <label>Mot de Passe</label>
                <input class="form-control" {{input_mdp}}>
                <span class="invalid-feedback">{{ Login.mdp_err }}</span>
            </div>
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Login">
            </div>
            <p>Don't have an account? <a href="creation_compte.php">Sign up now</a>.</p>
        </form>
    </div>
</body>
{% endblock main %}

